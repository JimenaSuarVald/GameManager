package modelo;

public class Administrador extends Persona {

    // Constructor del Administrador
    public Administrador(String nombre, String contraseña) {
        // Pasa los datos directamente a la clase madre (Persona)
        super(nombre, contraseña);
    }
    
    /* Aquí más adelante metemos mas métodos. 
     como por ejemplo: verListaDeTodosLosUsuarios();  o no se cual mas xd*/
    
}